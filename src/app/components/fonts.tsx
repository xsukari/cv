import { Sono, Open_Sans } from "next/font/google"

export const sono = Sono({ subsets: ["latin"] })
export const sonoLight = Sono({ subsets: ["latin"], weight: "300" })
export const openSans = Open_Sans({ subsets: ["latin"] })